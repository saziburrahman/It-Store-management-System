<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/bootstrap/bootstrap.min.css">
</head>
<body>
    <%- include('userDashboardNav.ejs') %>
    <div class="container">
        <h1 class="text-center">Product List</h1>
        <a href="/userAddProduct" class="btn btn-primary"> Add Product </a>

        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Category</th>
                    <th>Quantity</th>
                    <th>Purchage Date</th>
                    <th>Depriciation Value</th>
                    <th>Warrenty</th>
                    <th>Seller</th>
                    <th>Price</th>
                </tr>
            </thead>
            <tbody>
                <% if(products.length>0){ %>
                    <% for(let i=0;i<products.length;i++){ %>
                        <tr>
                            <td><%= products[i].productBrandName %></td>
                            <td><%= products[i].productCategory %></td>
                            <td><%= products[i].productQuantity %></td>
                            <td><%= products[i].productParchageDate.toDateString() %></td>
                            <td></td>
                            <td><%= products[i].productWarrenty %></td>
                            <td><%= products[i].productSeller %></td>
                            <td><%= products[i].productPrice %></td>
                        </tr>
                    <% } %>
                <% } else { %>
                    <p>No Registered Product</p>
                <% } %>
            </tbody>
        </table>
    </div>
</body>
</html>